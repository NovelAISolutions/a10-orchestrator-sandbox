document.addEventListener("DOMContentLoaded", function() {
  // Smooth scrolling is automatically handled by the CSS property 'scroll-behavior: smooth'.
  // Further enhancements and feature implementations can be added here.
});
